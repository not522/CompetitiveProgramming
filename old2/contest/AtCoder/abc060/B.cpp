#include "template.hpp"

int main() {
  setBoolName("YES", "NO");
  int a(in), b(in), c(in);
  for (int i = 1; i < b; ++i) {
    if (a * i % b == c) {
      cout << true << endl;
      return 0;
    }
  }
  cout << false << endl;
}

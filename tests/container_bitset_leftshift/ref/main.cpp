#include <iostream>

using namespace std;

int main() {
  int n, s;
  cin >> n >> s;
  for (int i = 0; i < s; ++i) {
    cout << 0 << endl;
  }
  for (int i = 0; i < n; ++i) {
    int b;
    cin >> b;
    cout << b << endl;
  }
}

#include "string.hpp"

int main() {
  String s(in), t(in);
  int a(in), b(in);
  String u(in);
  if (s == u) {
    cout << a - 1 << " " << b << endl;
  } else {
    cout << a << " " << b - 1 << endl;
  }
}

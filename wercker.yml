box: not522/rime-docker
command-timeout: 60
build:
  steps:
    - script:
        name: build
        code: |
          make -j `grep -c ^processor /proc/cpuinfo 2>/dev/null`
    - script:
        name: test
        code: |
          make test
